# Ranges In Py Lists [ ]
    # list of tags w/ 4 elements
tags = ['pyton', 'development', 'tutorials', 'code']

tag_range = tags[:] # = same as [0:]
                        # works on both side of the chain
print(tag_range) # prints all elements